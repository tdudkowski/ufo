import * as React from "react"
import Layout from "../components/layout"

const NotFoundPage = () => {
    return (
        <Layout>
            <p>not found</p>
        </Layout>)
}

export default NotFoundPage